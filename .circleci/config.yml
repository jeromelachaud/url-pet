version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.20.2

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: build
          command: yarn

      - run:
          name: unit tests
          command: yarn test:server
